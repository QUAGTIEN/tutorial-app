# Vercel Production Secret: paste the complete Firebase service-account JSON.
# Firebase Console → Project settings → Service accounts → Generate new private key.
# Never commit the downloaded JSON or your local .env file.
FIREBASE_SERVICE_ACCOUNT_JSON=
